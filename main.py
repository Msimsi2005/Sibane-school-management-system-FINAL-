# This is the entry point for the Sibane Pre School Management System.
from app import run_app  # Import the main app runner

if __name__ == "__main__":  # Check if script is run directly
    # Start the application loop defined in `app.run_app()`
    run_app()  # Call the main app function

